{
  "mcpServers": {
    "nullbridge-mcp": {
      "command": "node",
      "args": ["dist/index.js"],
      "env": {
        "NODE_ENV": "production",
        "THIRDWEB_CLIENT_ID": "${THIRDWEB_CLIENT_ID}",
        "THIRDWEB_SECRET_KEY": "${THIRDWEB_SECRET_KEY}"
      }
    }
  },
  "server": {
    "name": "NullBridge MCP",
    "version": "1.0.0",
    "description": "Cross-Chain Asset Bridge Agent using thirdweb - Bridge assets across multiple blockchain networks",
    "author": "ENVXX",
    "homepage": "https://null-bridge.vercel.app",
    "repository": {
      "type": "git",
      "url": "https://github.com/envexx/NullBridge.git"
    },
    "serverUrl": "https://null-bridge.vercel.app/mcp"
  },
  "tools": [
    {
      "name": "bridge_asset",
      "description": "Bridge/swap assets across different blockchain networks using thirdweb. Returns a confirmation URL for manual transaction confirmation.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "fromChainId": {
            "type": "number",
            "description": "ID of the source chain (e.g., 42161 for Arbitrum, 8453 for Base, 1 for Ethereum)"
          },
          "toChainId": {
            "type": "number",
            "description": "ID of the destination chain (e.g., 8453 for Base, 42161 for Arbitrum, 1 for Ethereum)"
          },
          "fromTokenAddress": {
            "type": "string",
            "description": "Address of the token to swap from (use 0xEeeeeEeeeEeEeeEeEeEeeEEEeeeeEeeeeeeeEEeE for native tokens like ETH, MATIC)"
          },
          "toTokenAddress": {
            "type": "string",
            "description": "Address of the token to swap to"
          },
          "amount": {
            "type": "string",
            "description": "Amount of tokens to swap in human-readable format (e.g., '0.5', '1.0')"
          },
          "toAddress": {
            "type": "string",
            "description": "Wallet address that will receive the assets (optional)",
            "pattern": "^0x[a-fA-F0-9]{40}$"
          }
        },
        "required": ["fromChainId", "toChainId", "fromTokenAddress", "toTokenAddress", "amount"]
      }
    },
    {
      "name": "get_portfolio",
      "description": "Get wallet balances (native tokens and ERC20 tokens) across multiple chains using thirdweb SDK.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "address": {
            "type": "string",
            "description": "Wallet address to check balances for",
            "pattern": "^0x[a-fA-F0-9]{40}$"
          },
          "chainIds": {
            "type": "array",
            "items": {
              "type": "number"
            },
            "description": "Array of chain IDs to check (e.g., [1, 137, 8453]). If not provided, checks all supported chains"
          },
          "tokenAddresses": {
            "type": "object",
            "description": "Optional: Object mapping chainId to array of ERC20 token addresses to check (e.g., {\"1\": [\"0x...\", \"0x...\"], \"137\": [\"0x...\"]})"
          }
        },
        "required": ["address"]
      }
    },
    {
      "name": "get_wallet_balance",
      "description": "Get wallet balance for a specific token (native or ERC20) on a specific chain using thirdweb SDK.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "address": {
            "type": "string",
            "description": "Wallet address to check balance for",
            "pattern": "^0x[a-fA-F0-9]{40}$"
          },
          "chainId": {
            "type": "number",
            "description": "Chain ID to check balance on (e.g., 1 for Ethereum, 137 for Polygon, 8453 for Base)"
          },
          "tokenAddress": {
            "type": "string",
            "description": "Optional: ERC20 token address. If not provided, returns native token balance (ETH, MATIC, etc.)"
          }
        },
        "required": ["address", "chainId"]
      }
    }
  ],
  "resources": [
    {
      "uri": "nullbridge://bridge/confirm",
      "name": "Bridge Confirmation",
      "description": "Bridge transaction confirmation page",
      "mimeType": "text/html"
    }
  ],
  "capabilities": {
    "tools": {},
    "resources": {},
    "prompts": {}
  },
  "supported_chains": [
    {
      "id": 1,
      "name": "Ethereum Mainnet"
    },
    {
      "id": 10,
      "name": "Optimism Mainnet"
    },
    {
      "id": 137,
      "name": "Polygon Mainnet"
    },
    {
      "id": 42161,
      "name": "Arbitrum One"
    },
    {
      "id": 8453,
      "name": "Base Mainnet"
    },
    {
      "id": 421614,
      "name": "Arbitrum Sepolia"
    },
    {
      "id": 84532,
      "name": "Base Sepolia"
    }
  ],
    "features": [
    "Cross-chain asset bridging",
    "thirdweb SDK integration",
    "Manual confirmation via redirect URL",
    "Multi-chain support",
    "Native token support",
    "Portfolio balance checking",
    "ERC20 token balance support"
  ]
}
